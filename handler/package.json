{
  "name": "handler",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "jest",
    "prebuild": "rm -rf dist",
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --format=cjs --outfile=dist/index.js",
    "postbuild": "cd dist && zip -r index.zip index.js*",
    "start": "node ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.41",
    "aws-sdk": "^2.954.0",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^4.1.7",
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^26.0.24",
    "@types/mongodb": "^4.0.7",
    "esbuild": "^0.18.12",
    "jest": "^26.6.3",
    "mongodb": "^5.7.0",
    "ts-jest": "^26.5.6",
    "ts-node": "^9.1.1"
  }
}
